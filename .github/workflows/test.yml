name: Prueba numero 1

on:
  push:
    branches:
      - main

jobs:
  decir-hola:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    environment: produccion  

    steps:
      - name: Mostrar mensaje
        run: echo "Hola Mundo"
      
      - name: Enviar Petición
        run: |
          curl -X POST 'https://webhook.site/892f2749-aba3-44c8-af63-04640edf7a85' \
          -H 'content-type: application/json' \
          -d '{"nombre": "Branlee"}'

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Logout previo de GHCR (Opcional)
        run: docker logout ghcr.io || true

      - name: Logearnos en GHCR
        run: docker login ghcr.io -u "${{ github.actor }}" -p "${{ secrets.TOKEN1 }}"

      - name: Crear imagen 
        run: docker build . -t ghcr.io/BranliPrime/express-image:latest

      - name: Desplegar imagen
        run: docker push ghcr.io/BranliPrime/express-image:latest
